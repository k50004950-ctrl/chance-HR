{
  "name": "attendance-system-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Attendance Management System - Monorepo",
  "type": "module",
  "scripts": {
    "start": "npm start --prefix backend",
    "build": "npm run build --prefix frontend && cp -r frontend/dist backend/",
    "install-all": "npm install --prefix frontend && npm install --prefix backend",
    "postinstall": "npm install --prefix frontend && npm install --prefix backend && npm run build"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}
