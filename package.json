{
  "name": "attendance-system-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Attendance Management System - Monorepo",
  "type": "module",
  "scripts": {
    "start": "npm start --prefix backend",
    "build": "npm run build --prefix frontend && cp -r frontend/dist backend/ && cp -r frontend/dist .",
    "install-all": "npm install --prefix frontend && npm install --prefix backend",
    "postinstall": "npm install --prefix frontend && npm install --prefix backend && npm run build",
    "test:load": "artillery run tests/load/scenarios.yml",
    "test:load:quick": "artillery quick --count 10 -n 20 http://localhost:3000/api/push/public-key",
    "test:load:auth": "artillery run tests/load/auth-flow.yml"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "artillery": "^2.0.30",
    "sharp": "^0.34.5"
  }
}
